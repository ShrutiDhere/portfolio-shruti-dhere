# Shruti Dhere Portfolio

This portfolio is ready to publish as a static website.

## Publish with GitHub Pages

1. Create a public GitHub repository named `portfolio-shruti-dhere`.

2. Add the remote and push from this folder:

```powershell
cd "c:\Users\dell\Desktop\portfolio-Shruti Dhere"
git push -u origin main
```

3. In your GitHub repository:
   - Open `Settings` → `Pages`
   - Under `Build and deployment`, choose `Branch: main` and `Folder: / (root)`
   - Save

4. After a minute, your public link will be:
   - `https://ShrutiDhere.github.io/portfolio-shruti-dhere/`

## Notes

- `index.html` must remain at the root of the repository.
- All site files are already committed and ready for publish.
- If you need help pushing, paste the GitHub repository URL and I can give you the exact command.
